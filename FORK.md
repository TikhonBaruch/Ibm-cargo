# LBM workspace

Каталог сборки продукта **LBM** (`Ibm-cargo`) — **отдельный** от taurus/llm.  
Приложение: корневой Next (`app/` + `src/`) · AI Compose: `containers/{llm,ocr}` · инфра: своя БД + S3 `lbm`.  
Матрица AI — другой проект; в этот репозиторий **не** клонировать (D36).
