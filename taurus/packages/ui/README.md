# packages/ui

Общие design tokens LBM Брокер.

- `src/tokens.css` — CSS variables (`--kb-blue`, шрифты, радиусы)
- Подключение: импорт в глобальные стили или копирование токенов в `app/globals.css` / `tailwind.config.ts`

Канон: `docs/design/refs/` и `docs/knowledge/design.md`.
