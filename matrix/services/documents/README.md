# services/documents

AI Documents pack validation stub (landing: errors before customs).  
Contract: `contracts/d-documents.llm.json`. Future env: `DOCUMENTS_SERVICE_URL`.  
Planned depth: combine with OCR + classification (not a third FSM).

| | |
|--|--|
| Port | `4750` |
| Validate | `POST /v1/validate` |
| Health | `GET /health` |
