# cabinets/ (archived HTML port)

Previously hosted a static HTML port of `cargo-broker-cabinets.html`.
**Live product cabinets** are React: `src/components/ved/*Cabinet` + `VedShell`.

`ADMIN_CABINET_PATHS` moved to [`src/lib/ved/admin-paths.ts`](../../lib/ved/admin-paths.ts).

Do not revive `CabinetsApp` / markup as the product face (ADR D6).
