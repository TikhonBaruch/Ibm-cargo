# План: точная копия шага заявки «Что ввозите?» (C10)

**D33.** Live `/cabinet/new`. Канон визуала: скрин макета + [`src/lbm-bro/components/client-wizard.tsx`](../../src/lbm-bro/components/client-wizard.tsx) шаг 1 (first-free).  
Поля: [`calculation-fields.md`](./calculation-fields.md). Chrome: [`plan-lbm-bro-visual.md`](./plan-lbm-bro-visual.md).

## 1. Идея

Собрать **точную копию** первого экрана мастера заявки (kicker «первый код бесплатный», шаги Товар / Бесплатно / Код, баннер 0 ₽, режим, фото, наименование+описание, сайдбар «По заявке»).

**Остальной функционал временно скрыть** в UI: тарифы D10, CSV/мультипозиция, инвойс/qty/вес (уже C8), select страны, attrs, HS-autocomplete, выбор брокера, шаги 2–3 как рабочие экраны.

Domain create / D8 / D10 / D11 **не менять**. «0 ₽» и «Бесплатно» — chrome макета. Оплата после создания — как сейчас (тариф формы, default STANDARD).

## 2. Что на экране (лок)

| Блок | Копия |
|------|--------|
| Назад | `← На главную` → `/cabinet` |
| Kicker | Первый код бесплатный · этот просчёт только ТН ВЭД |
| H2 | Что ввозите? |
| Шаги | 1 Товар (on) · 2 Бесплатно · 3 Код — клик по 2/3 no-op |
| Lead | Одна позиция: описание товара и документы для кода ТН ВЭД. |
| Баннер | `1 бесплатно` / Первый просчёт — 0 ₽ / пакеты Код·Таможня·Под ключ (текст макета) |
| Режим | Одна позиция · 1 бесплатно (on). Мультипозиция — видима, без пакета/CSV |
| Фото | dropzone «Перетащите фото…» / Выбрать файлы / Снять фото → live `onUpload` |
| Текст | одно поле «Наименование и описание» |
| Страна | скрыта; sidebar «Китай»; create шлёт origin CN + composition из описания |
| Сайдбар | синяя «Первый просчёт · 0 ₽» + 4 белые черты (не HS) + «По заявке» |
| CTA | Далее (ниже фолда макета) → live create |

## 3. Не делать

Бэкенд required; fake free pay; шаг «Бесплатно»/«Код» как рабочие экраны; KPI админа; manufacturer chrome.

## 4. Проверка

Unit: исходник NewCalcPane содержит kicker/баннер/шаги и не содержит `tariff-mini` / CSV / «Страна происхождения».  
`npm run test:ci`. Ручной: `/cabinet/new` = макет (фото → описание, сайдбар 4 черты); create по «Далее» всё ещё живой.

Restore: вернуть полный NewCalcPane (тарифы, позиции, attrs) из git до C10.
